- desc: it should run java
  command: [which java]
  entrypoint: [bash, -i, -c]
  assert:
  - status == 0
  - stderr.indexOf("java") != -1
# - desc: it should run maven
#   command: [mvn -v]
#   entrypoint: [bash, -i, -c]
#   assert:
#   - status == 0
#   - stdout.indexOf("Apache Maven") != -1
# - desc: it should run sdk
#   command: [sdk v]
#   entrypoint: [bash, -i, -c]
#   assert:
#   - status == 0
#   - stdout.indexOf("SDKMAN") != -1